need to run : npm install
need to run : npm install opentok
need to modify: var OpenTok = require('opentok'); in index.js
need to modify the port :
const PORT = process.env.PORT || 3000

report the fixed port
